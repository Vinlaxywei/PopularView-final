<?xml version="1.0" encoding="utf-8"?>
<transitionSet xmlns:android="http://schemas.android.com/apk/res/android"
    android:duration="250"
    android:transitionOrdering="together">
    <fade>
        <targets>
            <target android:excludeId="@android:id/statusBarBackground" />
            <target android:excludeId="@id/detail_poster_imageview" />
        </targets>
    </fade>

    <slide android:slideEdge="top">
        <targets>
            <target android:targetId="@+id/toolbar"/>
        </targets>
    </slide>

    <slide android:slideEdge="right">
        <targets>
            <target android:targetId="@id/detail_card_descView"/>
        </targets>
    </slide>

    <slide android:slideEdge="right">
        <targets>
            <target android:targetId="@id/detail_card_likeView"/>
        </targets>
    </slide>

    <slide android:slideEdge="bottom">
        <targets>
            <target android:targetId="@id/detail_movie_summaryText"/>
        </targets>
    </slide>

    <slide android:slideEdge="bottom">
        <targets>
            <target android:targetId="@id/detail_card_summaryView"/>
        </targets>
    </slide>

</transitionSet>